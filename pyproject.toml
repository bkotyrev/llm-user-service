[project]
name = "llm-user-service"
version = "1.0.0"
description = "Минимальный сервис суммаризации текста через LLM"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "httpx>=0.27.0",
  "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
